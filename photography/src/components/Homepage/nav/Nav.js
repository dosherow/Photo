import styled from 'styled-components';

const Nav = styled.div`
    display: block;
	width: 100%;
	position: fixed;
	text-transform: uppercase;
	top: 25px;
	z-index: 1000;
	opacity: 1;
`;

export default Nav;