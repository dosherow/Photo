import styled from 'styled-components';

const Grid10 = styled.div`
    position: absolute;
	transform-origin: left;
	height: 1px;
	top: calc(var(--vh,1vh)*33.3333);
	width: 100%;
	background-color: rgba(6, 6, 6, .8);
`;

export default Grid10;